"""
MedPilot OS — MCP (Model Context Protocol) Server Layer
Each module wraps an external API domain with typed tool definitions.
Agents import and call these tools instead of inline httpx requests.
"""
